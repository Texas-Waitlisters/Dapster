// If "artist" then pull from API
// else pull user info from FB
// Profile contains social media, bio, other songs
import React from 'react';

export default (props) => {
  return (
    <div id="profile">
      <h1> My name </h1>
      <h3> Total HAshes </h3>
    </div>
  );
}